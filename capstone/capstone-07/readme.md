Server:

Express Routes in API flashcards-routes.js are endpoints for decks/cards

auth-routes.js are for logging in, logging out, and registering. IMPORTANT: DOES NOT MANIPULATE DATABASE DIRECTLY, instead calls service layer functions
